# 真实模型与预测提交验收 · 2026-09-17

使用用户已有的 PathMNIST / FedAvg 最终训练 checkpoint `task04.pt`，分别完成模型提交和该模型预测文件提交。没有重新训练，也没有使用随机权重替代。

## 输入与执行

- 原模型为 CIFAR 风格 ResNet-18，11,173,449 个参数，上传原始 checkpoint 为 89,532,562 bytes。使用 `model` 字段内的参数，保留 `weights_only=True` 和严格加载。
- 预测包由原训练代码的 `backbone.ResNet18.resnet18` 生成，使用 PIL Resize 128×128、ToTensor 和全部 9 类 argmax。覆盖现有冻结测试集全部 7,180 张图像，NPZ 为 24,921 bytes。
- 平台适配后模型与原代码在三个任务各两张图像上的 logits 最大绝对差为 `4.76837158203125e-07`，通过 `atol=rtol=1e-5` 检查；另有四张图像通过实际隔离进程的预测标签一致性检查。
- 两份文件通过运行在线上代码、配置及状态目录的 Streamlit AppTest 表单上传，点击提交并进入现有 supervisor 评分队列。模型在 Linux Landlock + seccomp 隔离子进程中执行；原始代码生成参考预测时使用 GPU，平台评分使用 CPU。
- 浏览器检查结果记录；两种模式均正常展示完成状态，CSV、JSON、HTML 报告均成功生成且不含服务器资产路径。

## 完成记录

| 提交方式 | 记录 ID | 提交时间 UTC | 完成时间 UTC |
|---|---|---|---|
| 最终模型 | `f53d46df7b7f4e53a76650108890f981` | 08:16:45 | 08:22:40 |
| 完整预测 | `62a8f6582317426693022fb3ed78ed12` | 08:16:48 | 08:22:42 |

| 当前平台任务 | 图像数 | 模型提交 Accuracy | 预测提交 Accuracy |
|---|---:|---:|---:|
| C1 / 类别 0–2 | 2,524 | 0 | 0 |
| C2 / 类别 3–5 | 2,261 | 0 | 0 |
| C3 / 类别 6–8 | 2,395 | 0.5578288100208768 | 0.5578288100208768 |
| 最终任务宏平均 | 7,180 | 0.1859429366736256 | 0.1859429366736256 |

全部评分单元、结果矩阵和持续学习指标字段逐项相同。样本加权 Accuracy 同为 `0.1860724233983287`；只提交最终阶段，因此 BWT、遗忘等缺少历史的指标仍为空。

这里采用现有平台三任务协议和全局九类别输出。原训练 checkpoint 来自四任务 `[2,2,2,3]` 协议，其记录采用已知任务类别掩码；本表不与原训练日志的准确率直接比较，也不作为原论文协议复现。零分是本次完整全局类别评测的实际结果，未修饰或替换。

## 本次修复与检查

补齐该 checkpoint 的结构命名和原预处理适配；给有界 CPU 推理留出足够时间，修正评分进程向子进程继承 CPU 硬上限的问题。文件、网络和进程隔离保持开启。浏览器检查还复现了记录状态更新后第一次切换会跳回旧记录的问题，使用稳定的记录选择键修复，无新增页面选项或布局变化。

检查包括本机 4 项模型/隔离、6 项界面、14 项平台检查，以及 Linux 4 项模型/隔离检查。界面回归包含状态变化后的记录切换、预测 T4 仅覆盖 T1–T4，以及最终模型覆盖六任务。

此次真实权重端到端验收范围为 **分类**。后续真实分割验收见 [SEGMENTATION_SUBMISSION_VALIDATION.md](SEGMENTATION_SUBMISSION_VALIDATION.md)；体配准权重尚未完成端到端验收。私有 checkpoint、完整预测包、原始影像、标签、服务器路径及原始运行日志不提交到 GitHub。
