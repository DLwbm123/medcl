# 首页按钮与平台文字层级修复

2026-09-07；起始本地与远端 SHA：`5f250d979cd1892b3d7c24e19843522a4ea6a72c`。

首页三个任务卡片的旧 CSS 隐藏了卡片内所有按钮文字，新增的四个示例入口因此只剩箭头。现在把隐藏文字与箭头样式限定在 `home-enter-*` 三个紧凑入口，四个 `home-showcase-*` 按钮显示完整文字、浅蓝背景及边框。原生可访问名称、点击回调保持不变。

主导航由 14px 调整为 18px / 700 字重，手机宽度为 14px / 700。统一加强页面标题、子标题、表单标签、按钮、指标数字；首页任务与分区标题、最近记录的协议名、分类示例结果另按其原有样式调整。正文与辅助说明继续保留较轻层级。

验证：

- `test_homepage_ui.py` 16 项、`test_app.py` 5 项、`test_showcase.py` 4 项，共 25 项通过；使用已有 `/opt/miniconda3/bin/python3` 环境。
- 重启实际 Streamlit 8501。检查首页、分割任务中心、评测记录、方法比较入口及分类示例；实际按钮可点击。
- 浏览器计算样式：四个示例按钮文字 `clip-path: none`、16px / 650；桌面四项导航 18px / 700。
- 1280px 桌面、640px 和 390px 窄屏检查，页面宽度未超过视口；窄屏导航与示例按钮文字可见。临时视口尺寸已恢复。
- 实际截图保存在本机 `.local/typography-evidence/`，未公开病例图片。本轮仅 Python 内嵌样式及 CSS 修改，没有修改 TypeScript 或生产查看器 bundle。

未逐一检查全部历史评测的长标题或所有浏览器；没有重跑模型评测及前端三维渲染验收。
